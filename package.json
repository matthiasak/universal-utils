{
  "name": "universal-utils",
  "version": "0.0.3",
  "description": "functional, event, storage, cache, and other utilities",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --stage 0 -d dist",
    "watch": "babel src --stage 0 -w -d dist"
  },
  "author": "Matt Keas (@matthiasak)",
  "license": "MIT",
  "dependencies": {
    "localforage": "^1.3.0",
    "node-fetch": "^1.3.3",
    "redis": "^2.2.5",
    "whatwg-fetch": "^0.10.0"
  },
  "devDependencies": {
    "babel": "^5.8.23",
    "regenerator-runtime": "0.0.1"
  }
}
